# 1121: 继承与派生04：构造和析构的调用顺序

题目链接: http://59.73.87.16/problem.php?id=1121

## 题目描述

已知Base为基类，派生出Derived类，两个类的定义及main的代码如下（不允许改动），请完成Base类和Derived类的构造函数和析构函数，能够根据输入获取相应的输出。

class Base

{

private:

       int b;

public:

       Base(int);

       ~Base();

};

class Derived:public Base

{

private:

       int d;

public:

       Derived(int,int);

       ~Derived();

};

int main()

{

       int a,b;

       cin>>a>>b;

       Derived dr(a,b);     

       return 0;

}

## 样例输入 
								Copy

1 3

## 样例输出								Copy

Base 1 says hello
Derived 3 says hi
Derived 3 says bye
Base 1 says goodbye

