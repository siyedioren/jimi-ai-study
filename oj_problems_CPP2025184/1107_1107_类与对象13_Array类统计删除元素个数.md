# 1107: 类与对象13：Array类统计删除元素个数

题目链接: http://59.73.87.16/problem.php?id=1107

## 题目描述

数组类Array用于存储一组双精度浮点数，在现有类定义基础适当增加必要的成员函数，完成下列任务：

main函数中输入任意个(个数不超过100)双精度浮点数（以0结束），将这些数存储到同一个Array类对象中，并调用displayArray输出这些数。再输入删除的元素x，从对象中删除所有的值为x的元素，再将剩余元素按照从小到大的顺序排序，并输出排序后结果。





已知数组类的定义（其它成员函数请根据需要添加）如下：





const int MaxSize=100;  //数组中元素的最大个数

 









class Array









{

public:

       Array ();      //初始化length=0

       int getLength();   //返回数组中实际元素个数

       void insertX(int i, double x);  //在下标i处插入x





       void displayArray();       //输出数组中有效元素  







       void mySort();//排序



       int deleteAllX(double x);//删除重复的X值



        

private:

       double data[MaxSize];  //存储元素

       int length;              //数组中有效元素个数

};

主函数如下（不允许改动主函数中代码）：

int main()

{

       int i=0;

double num;

       Array a; //顺序表的初始化

       while(1)

       {

              cin>>num;

              if(!num) break;

              try{

                     a.insertX(i++,num);  //insert

              }

              catch(const char *ms)

              {

                     cout<<ms<<endl;

              }

       }

       a.displayArray ();

       double x;

       cin>>x;

       int n=a.deleteAllX(x);   //delete all x

       cout<<'Count of deleted elements:'<<n<<endl;

      

       a.mySort();   //sort

       a.displayArray();

    return 0;

}

## 样例输入 
								Copy

23 2 2 344 3 32 2 5 0 2

## 样例输出								Copy

Length:8
Elements:23 2 2 344 3 32 2 5 
Count of deleted elements:3
Length:5
Elements:3 5 23 32 344

